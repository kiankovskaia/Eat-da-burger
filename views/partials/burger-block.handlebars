  
<li>
    {{burger_name}}

    <button class="devour btnstyle" data-id="{{id}}" data-justdevoured="{{devoured}}">
        {{#if devoured}}Devour it!{{else}}Ready to Re-Order?{{/if}}
    </button>
    <button class="delete-burger btnstyle2" data-id="{{this.id}}"> No Longer Available</button>
</li>
